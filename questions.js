const scienceQA = []; // an array of objects where every abject contain the category, questions, answers ans choices
const geoQA = [];
const artQA = [];
const historyQA = [];

class Question {
  constructor(category) {
    this.category = category;
  }
}
